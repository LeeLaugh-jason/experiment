# 实验设计方案：移动端按钮大小与间距对点击效率与误触率的影响

## 1. 研究概述

### 1.1 研究题目
**移动端按钮大小与间距对点击效率和误触率的影响**

### 1.2 研究背景与意义

在移动端界面设计中，按钮（可点击控件）的大小和间距是影响用户交互绩效的核心设计参数。Fitts 定律指出，目标的大小和距离直接影响指向操作的完成时间——目标越小、距离越远，操作时间越长。然而，Fitts 定律主要关注单一目标场景；在移动端多按钮密集布局的实际界面中，按钮间距（即相邻目标之间的间隔）对点击效率的影响机制，以及按钮大小与间距之间可能存在的交互效应，尚需系统研究。

本研究通过控制变量实验，系统考察移动端按钮大小和间距两个关键设计参数对用户点击绩效的影响，为移动端界面设计的尺寸与间距规范提供实证依据。

### 1.3 研究问题

1. **按钮大小主效应**：不同大小的按钮对点击操作时间和误触率是否有显著影响？
2. **按钮间距主效应**：不同间距条件下，点击操作时间和误触率是否显著不同？
3. **交互效应**：按钮大小与间距之间是否存在交互作用——例如，在间距过小时，大按钮是否反而更容易引发误触？
4. **预测建模**：能否用按钮面积和间距等客观参数有效预测操作时间和误触发生概率？

---

## 2. 研究假设

### 2.1 虚无假设（H₀）

- **H₀1**：按钮大小对操作时间无显著影响（μ_小 = μ_中 = μ_大）
- **H₀2**：按钮间距对操作时间无显著影响（μ_紧凑 = μ_适中 = μ_宽松）
- **H₀3**：按钮大小对误触率无显著影响
- **H₀4**：按钮间距对误触率无显著影响
- **H₀5**：按钮大小与间距之间无交互效应

### 2.2 备择假设（H₁）

- **H₁1**：按钮大小显著影响操作时间（预期：按钮越大，操作时间越短）
- **H₁2**：按钮间距显著影响操作时间（预期：间距适中或宽松时操作时间更短）
- **H₁3**：按钮大小显著影响误触率（预期：按钮过小或过大均可能增加误触）
- **H₁4**：按钮间距显著影响误触率（预期：间距过小导致误触率上升）
- **H₁5**：按钮大小与间距之间存在交互效应（预期：间距过小时大按钮的误触率可能高于预期）

---

## 3. 实验设计

### 3.1 设计类型

采用 **3（按钮大小）× 3（按钮间距）两因素被试内设计**（全析因设计）。

所有被试需完成全部 9 种实验条件组合。实验顺序采用**平衡拉丁方设计**（Latin Square Counterbalancing），以抵消顺序效应、练习效应和疲劳效应。

### 3.2 自变量

| 自变量 | 类型 | 水平 |
|--------|------|------|
| **因素 A：按钮大小** | 分类变量（被试内） | 小（40×40 dp）、中（60×60 dp）、大（80×80 dp） |
| **因素 B：按钮间距** | 分类变量（被试内） | 紧凑（5 dp）、适中（15 dp）、宽松（30 dp） |

> **注**：dp（Density-independent Pixel）为 Android 系统的密度无关像素单位，1 dp ≈ 1 px（在 mdpi 屏幕下），本研究在固定设备/屏幕密度下测试，dp 直接对应物理尺寸。

**实验条件组合（3 × 3 = 9 种）：**

| 条件编号 | 按钮大小 | 按钮间距 |
|:--------:|:--------:|:--------:|
| 1 | 小（40×40） | 紧凑（5） |
| 2 | 小（40×40） | 适中（15） |
| 3 | 小（40×40） | 宽松（30） |
| 4 | 中（60×60） | 紧凑（5） |
| 5 | 中（60×60） | 适中（15） |
| 6 | 中（60×60） | 宽松（30） |
| 7 | 大（80×80） | 紧凑（5） |
| 8 | 大（80×80） | 适中（15） |
| 9 | 大（80×80） | 宽松（30） |

### 3.3 因变量

| 因变量 | 类型 | 测量方式 |
|--------|------|----------|
| **操作时间（Response Time, RT）** | 连续变量（ms） | 从目标按钮出现到被试成功点击目标按钮的时间间隔 |
| **误触率（Error Rate, ER）** | 连续变量（%） | 点击非目标按钮的次数占总点击次数的比例 |
| **主观负荷评分** | 顺序变量 | NASA-TLX 简化版量表评分（1–20 分制） |

### 3.4 控制变量

为排除额外变量对实验结果的影响，以下因素在实验中保持恒定或加以控制：

| 控制变量 | 控制方式 |
|----------|----------|
| **设备类型** | 统一使用同一型号手机/平板，屏幕尺寸与分辨率固定 |
| **目标颜色与对比度** | 固定目标按钮颜色（如红色），背景色与对比度一致 |
| **任务难度** | 均为单一目标点击任务，目标按钮位置随机呈现 |
| **环境光照** | 实验室环境照度恒定（~500 lux） |
| **被试姿势** | 统一为坐姿单手操作 |
| **实验时间** | 每名被试在同一时段完成实验，避免昼夜节律影响 |
| **刺激呈现时间** | 每次试次（trial）的响应窗口时间固定 |

---

## 4. 被试

### 4.1 样本量估计

采用 **G\*Power** 软件进行先验功效分析（a priori power analysis）。

- **统计方法**：重复测量方差分析（Repeated measures ANOVA），两因素被试内设计
- **效应量**：`f = 0.25`（中效应）
- **显著性水平**：`α = 0.05`
- **检验功效**：`1 − β = 0.80`
- **组间相关性**：`r = 0.5`
- **球形度校正**：`ε = 1`

计算结果：建议招募 **≥ 30 名有效被试**。

### 4.2 被试招募标准

- 年龄 18–35 岁
- 日常使用智能手机（触屏操作经验 ≥ 1 年）
- 右利手（统一操作手以减少变异）
- 裸眼或矫正视力正常（无色盲、色弱）
- 此前未参与过同类实验

---

## 5. 实验材料与设备

### 5.1 硬件设备

- **实验用手机**：统一品牌型号（如 iPhone 12 / 小米 11 或同等），屏幕尺寸约 6.1–6.7 英寸，分辨率 ≥ 1080 × 2400
- **固定支架**：手机固定于桌面支架，统一观看距离约 30–40 cm
- **数据记录计算机**：记录日志数据

### 5.2 实验软件

- **实验程序**：使用 **HTML / JavaScript** 构建 Web 端实验页面（亦可选用 PsychoPy 或原生 App）
- **日志记录**：自动记录每试次的点击坐标、目标位置、响应时间、是否误触等
- **数据分析**：SPSS、R、Python（scikit-learn、pandas、statsmodels）

### 5.3 主观问卷

- **NASA-TLX 简化版**：评估被试在每种条件下的主观脑力负荷（包含脑力需求、体力需求、时间需求、绩效、努力、挫折感 6 个维度）

---

## 6. 实验任务与流程

### 6.1 实验任务设计

实验采用**目标点击任务**（Target Acquisition Task）：

1. 屏幕首先显示一个 **起始点**（如中央十字），被试点击起始点开始试次
2. 短暂间隔后，屏幕上出现若干彩色按钮（包含一个目标按钮和若干干扰按钮），目标按钮以指定颜色（如红色）标识
3. 被试需尽快点击目标按钮
4. 点击后自动进入下一试次

**按钮布局**：在屏幕上以网格/环形方式排列若干按钮，目标按钮位置在每次试次中随机变化，被试无法预测。

**试次安排**：每种实验条件（9 种）包含 10 次有效试次，合计 9 × 10 = **90 次/人**。

### 6.2 实验流程

```
步骤 1 ─── 欢迎与知情同意（2 min）
步骤 2 ─── 人口学信息登记与实验说明（3 min）
步骤 3 ─── 练习阶段（1 min，非正式数据条件，2 次练习试次熟悉操作）
步骤 4 ─── 正式实验（约 15–20 min，按平衡拉丁方顺序执行 9 种条件）
步骤 5 ─── 每完成一种条件后填写 NASA-TLX 简化版（3 min × 9 = 27 min）
步骤 6 ─── 结束与致谢（2 min）
        ─────────────────────────────────────────
        预计总时长：约 50–60 min/人
```

### 6.3 顺序平衡设计

采用 **平衡拉丁方**（Balanced Latin Square）对 9 种条件进行顺序平衡：

- 每位被试以不同的随机顺序执行 9 种条件
- 每个条件在各顺序位置出现的频次相等
- 每个条件在其他每个条件之前和之后出现的频次也相等
- 以此消除**顺序效应**（order effect）、**练习效应**（learning effect）和**疲劳效应**（fatigue effect）

---

## 7. 数据处理

### 7.1 数据清洗

| 处理步骤 | 标准 |
|----------|------|
| **逻辑检查** | 剔除因程序故障导致的异常记录 |
| **离群值处理** | 操作时间超出各条件均值 ± 3 SD 或超出 P25/P75 四分位距 1.5 倍以上的数据视为离群值，予以剔除 |
| **极端值处理** | 操作时间超出四分位距 3 倍以上的极端值，予以剔除 |
| **试次筛选** | 仅保留成功点击目标按钮的试次；首次点击即命中记为正确，首次点击非目标记为误触 |
| **缺失值处理** | 若某被试某种条件下有效试次不足 50%，则整条记录删除 |

### 7.2 数据汇总

每位被试最终形成 9 条条件均值记录：

- 每种条件的 **平均操作时间**（正确试次）
- 每种条件的 **误触率**（误触试次数 / 总试次数）
- 每种条件的 **NASA-TLX 总分**

---

## 8. 数据分析方法

### 8.1 描述性统计

- 计算各条件下因变量的均值（M）和标准差（SD）
- 绘制 **均值柱状图** 和 **箱线图**，直观展示分布趋势
- 统计表采用三线表格式

### 8.2 前提条件检验

在执行参数检验前，对数据进行以下检验：

1. **正态性检验**：Shapiro-Wilk 检验各条件残差是否符合正态分布
2. **方差齐性检验**：Levene 检验组间方差是否齐性
3. **球形度检验**：Mauchly 球形检验（若违反则采用 Greenhouse-Geisser 校正）

若前提条件不满足，采用非参数检验（Friedman 检验）或数据变换（如对数变换）。

### 8.3 两因素重复测量方差分析（核心分析）

采用 **3 × 3 两因素重复测量方差分析**（Two-way Repeated Measures ANOVA），分别对以下因变量进行分析：

| 因变量 | 分析模型 |
|--------|----------|
| 操作时间 | 两因素被试内 ANOVA（A × B） |
| 误触率 | 两因素被试内 ANOVA（A × B） |
| NASA-TLX | 两因素被试内 ANOVA（A × B） |

**分析内容**：

- **主效应**：按钮大小（A）的主效应、按钮间距（B）的主效应
- **交互效应**：A × B 的交互效应
- **简单效应分析**：若交互效应显著，进行简单效应分析（Simple Effects Analysis）
- **事后多重比较**：主效应显著时，采用 **Bonferroni 校正** 进行事后两两比较

### 8.4 回归分析

以操作时间为因变量，建立**多元线性回归模型**：

```
RT = β₀ + β₁ × Area + β₂ × Spacing + β₃ × (Area × Spacing) + ε
```

- 其中 `Area` 为按钮面积（dp²），`Spacing` 为间距（dp）
- 通过回归系数判断各变量的贡献大小
- 检验回归模型的显著性（F 检验）和拟合优度（R²）

### 8.5 机器学习分析

采用**随机森林分类模型**（Random Forest Classifier），以试次级别数据预测**是否发生误触**：

**特征工程**：

| 特征 | 说明 |
|------|------|
| 按钮面积 | 目标按钮的像素面积 |
| 邻近距离 | 最近干扰按钮到目标按钮的距离 |
| 间距 | 当前条件的按钮间距值 |
| 目标位置_x | 目标按钮屏幕 x 坐标 |
| 目标位置_y | 目标按钮屏幕 y 坐标 |
| 相对位置 | 目标按钮在布局中的行列号 |

**模型设置**：

- 训练/测试集划分：80% / 20%
- 评估指标：准确率（Accuracy）、精确率（Precision）、召回率（Recall）、F1 分数、AUC
- **特征重要性排序**：输出各特征对误触预测的贡献度，验证间距是否是最重要的影响因素

### 8.6 统计软件

| 分析阶段 | 工具 |
|----------|------|
| 功效分析 | G\*Power |
| 数据清洗与整理 | Python（pandas） |
| 方差分析 | SPSS 或 R（ezANOVA / aov_car） |
| 回归分析 | R（lm）或 Python（statsmodels） |
| 机器学习 | Python（scikit-learn） |
| 可视化 | Matplotlib / Seaborn / ggplot2 |

---

## 9. 预期结果与贡献

### 9.1 预期结果

1. **按钮大小主效应**：按钮越大，操作时间越短，误触率越低（符合 Fitts 定律预测）
2. **间距主效应**：间距越大，误触率越低；但间距对操作时间的影响可能呈非线性
3. **交互效应**：当间距极小时，大按钮的有效"热区"可能相互重叠，反而增加误触风险——表现为大按钮在紧凑间距下的误触率异常升高
4. **回归预测**：按钮面积和间距能显著预测操作时间（R² > 0.6 的预期）
5. **机器学习**：间距在误触预测中的特征重要性排名靠前

### 9.2 实践贡献

- 为移动端界面设计规范（如 Material Design、iOS HIG）中按钮尺寸与间距建议提供实验验证
- 提供基于数据驱动的按钮布局优化建议
- 在设计空间受限（如小屏幕设备、密集信息界面）时提供量化参考

---

## 10. 参考文献

[1] Fitts, P. M. (1954). The information capacity of the human motor system in controlling the amplitude of movement. *Journal of Experimental Psychology*, 47(6), 381–391.

[2] ISO 9241-11 (2018). *Ergonomics of human-system interaction — Part 11: Usability: Definitions and concepts*.

[3] Nielsen, J. (1993). *Usability Engineering*. Academic Press.

[4] Montgomery, D. C. (2017). *Design and Analysis of Experiments* (9th ed.). Wiley.

[5] Hart, S. G., & Staveland, L. E. (1988). Development of NASA-TLX (Task Load Index): Results of empirical and theoretical research. *Advances in Psychology*, 52, 139–183.

[6] Breiman, L. (2001). Random forests. *Machine Learning*, 45(1), 5–32.

[7] Tao, D., et al. (2021). Effects of control-to-display gain and operation precision requirement on touchscreen operations in vibration environments. *Applied Ergonomics*, 91, 103293.

---

> **文件版本**：v1.0  
> **编制日期**：2026 年 6 月 10 日  
> **课程**：人机界面设计
