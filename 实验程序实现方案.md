# 实验程序实现方案：移动端按钮大小与间距点击实验

## 版本信息

| 项目 | 内容 |
|------|------|
| 文档版本 | v1.0 |
| 编制日期 | 2026 年 6 月 10 日 |
| 目标平台 | 移动端 Web（被试自带手机浏览器） |
| 数据方案 | 全客户端本地存储，手动导出 |

---

## 1. 架构概览

### 1.1 总体架构

```
┌─────────────────────────────────────────────────┐
│                  单页 Web 应用 (SPA)              │
│                                                   │
│  ┌─────────┐  ┌──────────┐  ┌─────────────────┐  │
│  │ 欢迎/   │  │ 实验页   │  │ 完成/数据导出   │  │
│  │ 信息页  │→ │ (9条件×  │→ │ 页面            │  │
│  │         │  │  20试次) │  │                 │  │
│  └─────────┘  └────┬─────┘  └─────────────────┘  │
│                     │                              │
│                     ▼                              │
│              ┌──────────────┐                      │
│              │  IndexedDB   │                      │
│              │  (本地存储)   │                      │
│              └──────────────┘                      │
└─────────────────────────────────────────────────┘
                        │
                  实验结束后手动导出
                        ▼
                 CSV / JSON 文件下载
```

### 1.2 技术栈

| 技术 | 选用原因 |
|------|----------|
| **纯 HTML5 + CSS3 + JavaScript** | 零依赖，无需构建工具，直接在手机浏览器打开 |
| **IndexedDB** | 存储大量试次级别数据（远超 localStorage 5MB 限制） |
| **`touch` 事件 API** | 原生触摸事件，精确获取点击坐标 |
| **Fullscreen API** | 隐藏浏览器 UI，最大化屏幕空间 |
| **PreventDefault 策略** | 禁止双指缩放、滑动翻页等干扰操作 |
| **Blob + `<a>` 下载** | 实验结束后一键导出 CSV |

> **不使用框架（React/Vue）**：单页面应用逻辑简单，原生 JS 即可胜任，且兼容性更好、体积更小。

### 1.3 浏览器兼容性目标

| 浏览器 | 支持情况 |
|--------|----------|
| Chrome for Android（一加内置浏览器 WebView 底层） | ✅ 完全支持 |
| Via 浏览器 | ✅ 支持（基于 WebKit，已验证 IndexedDB + Touch Events） |
| 系统自带浏览器（ColorOS 内置） | ✅ 支持 |

---

## 2. 关键参数解析

### 2.1 屏幕参数（一加 Ace 5 Pro 参考）

| 参数 | 值 |
|------|-----|
| 屏幕尺寸 | 6.78 英寸（对角线） |
| 物理分辨率 | 2780 × 1264 px |
| 像素密度 | 450 PPI |
| 典型 Device Pixel Ratio | ~3.0 |
| CSS 视口宽度 | ~421 px（1264 / 3） |
| 操作系统 | ColorOS 16.0.5（Android 16） |

### 2.2 dp 到 CSS px 的换算

> **核心结论：在 Android 手机上，Web 页面中 1 dp ≈ 1 CSS px，两者可直接等价使用。**

**推导过程**：

Android 的 dp（Density-independent Pixel）和浏览器的 CSS px 在 Android 手机上换算关系如下：

1. Android dp 定义：1 dp = 1/160 英寸的物理尺寸
2. Android 系统将屏幕按物理密度归入**密度桶**（density bucket）：
   - mdpi (160 PPI) → DPR = 1.0
   - hdpi (240 PPI) → DPR = 1.5
   - xhdpi (320 PPI) → DPR = 2.0
   - **xxhdpi (480 PPI) → DPR = 3.0** ← 一加 Ace 5 Pro 属于此桶
   - xxxhdpi (640 PPI) → DPR = 4.0
3. 浏览器使用系统的 DPR 来换算：**1 CSS px = DPR 个物理像素**
4. Android 系统使用相同的 DPR 来换算：**1 dp = DPR 个物理像素**

因此：**在同一个 Android 设备上，dp 和 CSS px 都映射到相同数量的物理像素，比例是 1:1。**

> **注意**：实际物理密度与密度桶的标称值可能有偏差（如一加 Ace 5 Pro 实际 450 PPI 被归入 480 PPI 的 xxhdpi 桶，偏差约 6%）。这种偏差在所有元素上一致，不会影响被试内比较。实验中每位被试完成全部 9 种条件，**相对差异不受影响**。

| 设计参数 | dp 值 | CSS px 值（代码中使用） | 一加 Ace 5 Pro 上物理尺寸 |
|----------|-------|------------------------|--------------------------|
| 按钮：小 | 40×40 | **40×40** | ~6.76×6.76 mm |
| 按钮：中 | 60×60 | **60×60** | ~10.1×10.1 mm |
| 按钮：大 | 80×80 | **80×80** | ~13.5×13.5 mm |
| 间距：紧凑 | 5 | **5** | ~0.84 mm |
| 间距：适中 | 15 | **15** | ~2.54 mm |
| 间距：宽松 | 30 | **30** | ~5.07 mm |

> **跨设备一致性**：由于"被试自带手机"，不同手机的 DPR 不同（如 2.75、3.0、3.5），按钮物理尺寸会因设备而异。但本实验采用被试内设计，每位被试的 9 种条件在同一台设备上完成，**跨设备的物理尺寸差异只增加随机误差，不会引入系统偏差**。在后端分析中可通过记录 DPR 作为协变量进行校正。

### 2.3 不同手机上的自适应策略

实验程序在加载时会自动执行以下检测：

```javascript
// 运行时检测示例
const dpr = window.devicePixelRatio;       // 如 3.0
const screenCSSWidth = window.screen.width / dpr;  // CSS 视口宽
const screenCSSHeight = window.screen.height / dpr; // CSS 视口高

// 用于后续计算：测试是否需要缩放以适应屏幕
```

**自适应规则**：

| 条件 | 处理方式 |
|------|----------|
| 屏幕 CSS 宽 ≥ 360px | 按钮按标准 0.6 系数换算，正常布局 |
| 屏幕 CSS 宽 < 360px（极小屏） | 按钮等比例缩放为 0.8 倍（在页面上给出提示） |
| 屏幕 CSS 宽 ≥ 500px（平板） | 按钮按标准系数，但布局居中在 420px 容器内 |

> **实际范围**：大部分现代手机 CSS 视口宽在 375–430px 之间（iPhone SE 为 375，一加 Ace 5 Pro 约 421，iPhone 16 Pro Max 约 430），无需额外缩放。

---

## 3. 页面结构设计

### 3.1 页面流程

```
┌─────────────────────────────────────────────────┐
│                  页面路由                         │
│                                                   │
│  / → 欢迎页面（知情同意 + 操作说明）              │
│  /register → 被试信息登记（编号/年龄/性别/惯用手） │
│  /practice → 练习阶段（2 次试次）           │
│  /experiment → 正式实验（9 条件 × 20 试次）       │
│  /break → 休息页面（每完成一个条件后展示）        │
│  /nasa-tlx → 主观负荷问卷（每条件后填写）         │
│  /complete → 完成页面 + 数据导出按钮              │
└─────────────────────────────────────────────────┘
```

所有页面在同一个 HTML 文件中通过 JavaScript 控制显示/隐藏（SPA 模式）。

### 3.2 页面内容概要

#### 3.2.1 欢迎页面
- 实验标题与简介
- 预计时长（约 25–30 分钟）
- 知情同意勾选框（必须勾选才能继续）
- 浏览器全屏提示

#### 3.2.2 被试登记页
- 被试编号（手动输入，或被试自己设定）
- 年龄（数字输入）
- 性别（单选）
- 惯用手（左/右，单选）
- 是否色盲色弱（筛查项，若"是"则无法继续实验）
- 日常手机使用时长（约 __ 小时/天）

#### 3.2.3 实验页
- 全屏沉浸模式（无浏览器 UI）
- 顶部显示当前条件编号（如 "条件 3/9"）和试次进度（如 "12/20"）
- 中央区域是按钮点击任务
- 无需其他多余 UI 元素

#### 3.2.4 NASA-TLX 问卷页
- 6 个维度各一个滑动条（1–20 分）
- 维度：脑力需求、体力需求、时间需求、绩效、努力、挫折感

#### 3.2.5 完成页
- 实验完成提示
- **数据导出按钮**：生成 CSV 文件并下载
- 退出全屏提示

---

## 4. 实验页核心交互设计

### 4.1 试次流程

```
┌─────────────────────────────────────────────────┐
│                 单个试次流程                       │
│                                                   │
│  1. 屏幕中央显示 ▶ 起始十字标记                    │
│  2. 被试点击起始十字                               │
│  3. 500ms 延迟（固定间隔）                          │
│  4. 按钮阵列出现（目标颜色 = 红色）：               │
│       ┌──┐  ┌──┐  ┌──┐                           │
│       │  │  │  │  │  │  ← 目标红色                 │
│       └──┘  └──┘  └──┘                           │
│       ┌──┐  ┌──┐  ┌──┐                           │
│       │  │  │● │  │  │  ← 其他为灰色               │
│       └──┘  └──┘  └──┘                           │
│       ┌──┐  ┌──┐  ┌──┐                           │
│       │  │  │  │  │  │                           │
│       └──┘  └──┘  └──┘                           │
│  5. 被试点击目标按钮（记录 RT 和命中/误触）         │
│     ↳ 若命中 → 短提示（50ms 绿色闪动）→ 进入下一试次 │
│     ↳ 若误触 → 不做反馈 → 被试继续寻找目标点击      │
│  6. 若超时（5000ms 未点击）→ 自动跳下一试次         │
└─────────────────────────────────────────────────┘
```

### 4.2 按钮布局方案

按钮以 **N × M 网格** 排列在屏幕中央区域。

**布阵规则**：

1. 网格行数和列数根据屏幕可用尺寸动态计算
2. 目标按钮随机出现在网格中的某个位置
3. 其余位置填充干扰按钮（灰色）
4. 网格实际占据的区域约为屏幕宽度的 60–70%，居中

**示例**（紧凑间距下 3px gap，3 列 × 3 行）：

```
   ┌──┐┌──┐┌──┐      ← 按钮 24×24px，间距 3px
   └──┘└──┘└──┘
   ┌──┐┌──┐┌──┐      ← 红色按钮随机出现在任一位置
   └──┘└──┘└──┘
   ┌──┐┌──┐┌──┐
   └──┘└──┘└──┘
```

**不同条件下的网格参数**：

| 条件（大小×间距） | 按钮 CSS px | 间距 CSS px | 建议网格 |
|:-----------------:|:----------:|:----------:|:--------:|
| 小×紧凑 | 24×24 | 3 | 4×4 = 16 按钮 |
| 小×适中 | 24×24 | 9 | 4×3 = 12 按钮 |
| 小×宽松 | 24×24 | 18 | 3×3 = 9 按钮 |
| 中×紧凑 | 36×36 | 3 | 3×3 = 9 按钮 |
| 中×适中 | 36×36 | 9 | 3×3 = 9 按钮 |
| 中×宽松 | 36×36 | 18 | 3×2 = 6 按钮 |
| 大×紧凑 | 48×48 | 3 | 3×3 = 9 按钮 |
| 大×适中 | 48×48 | 9 | 3×2 = 6 按钮 |
| 大×宽松 | 48×48 | 18 | 2×2 = 4 按钮 |

> 网格大小动态调整，保证所有按钮在屏幕可见范围内，且布局不超出屏幕宽度。

### 4.3 目标颜色设计

| 元素 | 颜色 |
|------|------|
| 目标按钮 | `#FF4444`（亮红色），正常辨识 |
| 干扰按钮 | `#CCCCCC`（浅灰色），与目标明显区分 |
| 起始十字 | `#333333`（深灰色） |
| 按钮文字（如果显示） | 无文字，纯色块 |
| 命中反馈 | 50ms 闪烁绿色 `#44FF44` |
| 背景 | `#F5F5F5`（浅灰白） |

> **色盲安全说明**：红-灰色组合对红绿色盲用户可能难以区分。实验登记页需筛查色盲/色弱被试，排除无法正常参与实验的个体。

### 4.4 时间记录

| 时间点 | 记录方式 |
|--------|----------|
| 试次开始 | 起始十字被点击的时刻 `performance.now()` |
| 按钮呈现 | 阵列渲染完成的时刻 `performance.now()` |
| 点击时间 | `touchstart` 事件触发时刻 `performance.now()` |
| 操作时间 RT | 按钮呈现时刻 − 成功点击目标时刻 |

> 使用 `performance.now()`（毫秒级高精度时间戳），而非 `Date.now()`。

---

## 5. 数据存储设计

### 5.1 数据库结构（IndexedDB）

```
数据库: ButtonExpDB
├── objectStore: participants
│   └── 每条记录:
│       ├── id: number (自增)
│       ├── participantId: string (被试编号)
│       ├── age: number
│       ├── gender: string
│       ├── handedness: string
│       ├── dailyPhoneUsage: number
│       ├── deviceInfo: {
│       │   ├── userAgent: string
│       │   ├── screenWidth: number (物理 px)
│       │   ├── screenHeight: number (物理 px)
│       │   ├── devicePixelRatio: number
│       │   └── cssWidth: number
│       │   └── cssHeight: number
│       ├── startedAt: string (ISO timestamp)
│       └── completedAt: string (ISO timestamp)
│
├── objectStore: trials
│   └── 每条记录:
│       ├── id: number (自增)
│       ├── participantId: string
│       ├── conditionIndex: number (0-8)
│       ├── buttonSize: string ('small'|'medium'|'large')
│       ├── buttonSpacing: string ('tight'|'moderate'|'loose')
│       ├── trialNumber: number (条件内试次序号 1-20)
│       ├── isPractice: boolean
│       ├── targetPosition: {row: number, col: number}
│       ├── targetScreenX: number (CSS px)
│       ├── targetScreenY: number (CSS px)
│       ├── hitPosition: {x: number, y: number} (实际点击 CSS px)
│       ├── hitTarget: boolean (是否命中目标)
│       ├── responseTime: number (ms，从按钮出现到点击)
│       ├── timeSinceTrialStart: number (ms)
│       └── timestamp: string (ISO timestamp)
│
├── objectStore: nasa_tlx
│   └── 每条记录:
│       ├── id: number (自增)
│       ├── participantId: string
│       ├── conditionIndex: number (0-8)
│       ├── mentalDemand: number (1-20)
│       ├── physicalDemand: number (1-20)
│       ├── temporalDemand: number (1-20)
│       ├── performance: number (1-20)
│       ├── effort: number (1-20)
│       └── frustration: number (1-20)
│
└── objectStore: experiment_state
    └── 会话状态（单条记录）:
        ├── participantId: string
        ├── currentPhase: string ('practice'|'experiment'|'complete')
        ├── currentCondition: number (0-8)
        ├── currentTrial: number
        └── conditionOrder: number[] (9 种条件的随机顺序)
```

### 5.2 状态恢复机制

如果被试不小心关闭了浏览器或页面，重新打开时可以从中断处继续：

```
1. 打开页面 → 检查 experiment_state 中是否存在未完成的记录
2. 若有 → 显示"检测到未完成的实验，是否继续？"对话框
3. 若选择继续 → 从断点处恢复（当前条件、当前试次）
4. 若选择重新开始 → 清空现有数据
```

---

## 6. 核心代码架构

### 6.1 文件结构

```
experiment/
├── index.html              ← 唯一 HTML 文件（所有页面 + CSS + JS 内嵌）
│
│   或者拆分（推荐，便于维护）：
│
├── index.html              ← HTML 结构
├── css/
│   └── style.css           ← 全部样式
├── js/
│   ├── app.js              ← 主控制器：页面路由、生命周期
│   ├── config.js           ← 实验参数配置（按钮大小、间距等）
│   ├── db.js               ← IndexedDB 封装
│   ├── experiment.js       ← 实验核心（试次逻辑、按钮渲染）
│   └── export.js           ← CSV 导出功能
└── README.md               ← 使用说明
```

> 实际部署时只需将这组文件放在任意静态 Web 服务器（或压缩后发给被试解压），用手机浏览器打开 `index.html` 即可。

### 6.2 关键模块说明

#### `config.js` — 实验参数配置

```javascript
// 实验设计的核心参数集中在此，便于修改
const CONFIG = {
  // 按钮大小 (dp) → CSS px = dp * 0.6
  buttonSizes: {
    small:  { dp: 40, css: 24, label: '小(40×40dp)' },
    medium: { dp: 60, css: 36, label: '中(60×60dp)' },
    large:  { dp: 80, css: 48, label: '大(80×80dp)' }
  },
  // 间距 (dp) → CSS px
  spacings: {
    tight:    { dp: 5,  css: 3,  label: '紧凑(5dp)' },
    moderate: { dp: 15, css: 9,  label: '适中(15dp)' },
    loose:    { dp: 30, css: 18, label: '宽松(30dp)' }
  },
  // 实验参数
  experiment: {
    practiceTrialsPerCondition: 2,
    formalTrialsPerCondition: 20,
    timeoutMs: 5000,           // 超时时间
    preStimulusDelayMs: 500,   // 起始点按下后到按钮出现的延迟
    hitFeedbackMs: 50,         // 命中反馈闪烁持续时间
    stimSize: '100%'           // 按钮占屏幕宽度比例（布局用）
  },
  // 目标颜色
  colors: {
    target: '#FF4444',
    distractor: '#CCCCCC',
    startCross: '#333333',
    hitFeedback: '#44FF44',
    background: '#F5F5F5'
  }
};
```

#### `experiment.js` — 核心逻辑流程图

```
renderStartCross()
    │
    ▼
等待 touchstart 在起始十字上
    │
    ▼
hideStartCross()
    │
    ▼
setTimeout(preStimulusDelayMs)  ← 固定 500ms 延迟
    │
    ▼
generateGridLayout(size, spacing, gridRows, gridCols)
    │
    ├── 随机选择目标位置 (row, col)
    ├── 计算所有按钮的 CSS 坐标 (left, top)
    │
    ▼
renderButtons(buttons, targetIndex)
    │
    ▼
记录 stimulusOnsetTime = performance.now()
    │
    ▼
等待 touchstart（全局捕获）
    │
    ├── 点击的是目标按钮？
    │   ├── YES → 闪烁反馈 → 记录命中 + RT → 下一试次
    │   └── NO  → 记录误触（不反馈）→ 继续等待点击目标
    │
    ├── 超时 5000ms？
    │   └── YES → 记录超时 → 下一试次
    │
    ▼
所有试次完成 → 切换到 NASA-TLX 问卷
    │
    ▼
所有条件完成 → 切换到完成页
```

#### `db.js` — IndexedDB 封装

提供以下 API：

| 方法 | 功能 |
|------|------|
| `DB.open()` | 打开/创建数据库 |
| `DB.saveParticipant(data)` | 保存被试信息 |
| `DB.saveTrial(data)` | 保存单次试次数据 |
| `DB.saveTLX(data)` | 保存 TLX 评分 |
| `DB.saveState(state)` | 保存实验进度 |
| `DB.getState()` | 获取实验进度 |
| `DB.clearState()` | 清除进度（实验完成时） |
| `DB.exportAll()` | 导出全部数据 |

#### `export.js` — 数据导出

```javascript
// 生成 CSV 格式的数据并下载
function exportData() {
  // 1. 从 IndexedDB 读取 participants, trials, nasa_tlx
  // 2. 拼接为 CSV 字符串（含表头）
  // 3. 创建 Blob 对象
  // 4. 创建 <a> 标签触发下载
  // 5. 文件名: button_exp_{participantId}_{日期}.csv
}
```

---

## 7. 关键实现注意事项

### 7.1 触摸事件 vs 鼠标事件

| 要点 | 处理方式 |
|------|----------|
| 核心事件 | 使用 `touchstart` 而非 `click`（click 有 300ms 延迟） |
| 多点触控 | 实验期间禁止双指操作：`touchstart` 时检查 `e.touches.length > 1` 则忽略 |
| 滚动/滑动 | `document.addEventListener('touchmove', e => e.preventDefault(), {passive: false})` |
| 长按 | 禁止长按菜单：`-webkit-touch-callout: none` + `user-select: none` |

### 7.2 页面缩放控制

```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
```

```css
/* 额外防止双指缩放 */
html {
  touch-action: none;
}
```

### 7.3 全屏模式

```javascript
// 进入全屏（在用户交互事件中调用）
function enterFullscreen() {
  const el = document.documentElement;
  if (el.requestFullscreen) {
    el.requestFullscreen();
  } else if (el.webkitRequestFullscreen) {  // iOS Safari
    el.webkitRequestFullscreen();
  }
}

// 注意：iOS Safari 的 Fullscreen API 支持有限
// Android Chrome / Via / 内置浏览器 → 良好支持
```

### 7.4 横屏锁定

通过 CSS 和 JS 引导用户使用**竖屏**模式：

```css
/* 提示用户在竖屏下使用 */
@media screen and (orientation: landscape) {
  .rotate-warning { display: flex; }
  .experiment-container { display: none; }
}
```

### 7.5 防页面退出

```javascript
// 实验进行中时，防止意外退出
window.addEventListener('beforeunload', (e) => {
  if (experimentInProgress) {
    e.preventDefault();
    e.returnValue = '实验正在进行中，确定要离开吗？';
  }
});
```

### 7.6 坐标获取

```javascript
// 获取触摸点在屏幕上的 CSS px 坐标
function getTouchPos(event) {
  const touch = event.touches[0] || event.changedTouches[0];
  return {
    x: touch.clientX,  // CSS px 坐标
    y: touch.clientY
  };
}
```

---

## 8. 部署方案

### 8.1 方案一：静态文件分发（推荐）

```
1. 将所有文件打包为 experiment.zip
2. 上传至任一方式：
   ├── a) GitHub Pages （免费，推荐）
   │   git push 到 GitHub，启用 Pages 即可获得 https://xxx.github.io/experiment/
   │   被试直接访问该链接
   │
   ├── b) 本地 HTTP 服务器（局域网）
   │   电脑上运行: python3 -m http.server 8080
   │   手机和电脑连同一 Wi-Fi，访问 http://电脑IP:8080
   │
   ├── c) 将文件发给被试自行解压
   │   被试解压后用手机浏览器打开 index.html 即可
   │   注意：部分浏览器通过 file:// 协议打开时 IndexedDB 可能受限
   │
   └── d) Vercel / Netlify（免费，一键部署）
       拖拽文件夹到部署页面即可生成公网链接
```

### 8.2 方案二：二维码分发（配合 GitHub Pages）

```
┌─────────────────────────────────────────────┐
│  1. 部署到 GitHub Pages                      │
│  2. 用二维码生成工具将链接转为二维码          │
│  3. 打印二维码贴在实验说明材料上              │
│  4. 被试扫码 → 直接打开实验                  │
└─────────────────────────────────────────────┘
```

### 8.3 数据回收

实验结束后，被试点击"导出数据"按钮：

```
1. 浏览器自动生成 CSV 文件
2. 触发下载到手机本地
3. 被试通过微信/QQ/邮件等方式将 CSV 文件发回给实验者
```

---

## 9. 需要你确认的参数

在开始写代码之前，请确认以下参数：

| 参数 | 你的选择 |
|------|----------|
| **网格布局** | 按钮排列成规则的网格（如上方案 3×3、4×3 等），还是环形/随机散落？→ **建议网格** |
| **目标颜色** | 红色目标 + 灰色干扰，如上方案→ **确认** 或有其他偏好？ |
| **试次数** | 每种条件 10 次试次（共 90 次） |
| **超时时间** | 5000ms 够不够？→ 预实验中可调整 |
| **起始十字** | 是否需要在每次试次前点击起始十字？还是直接在屏幕上呈现按钮？→ **需要起始十字可固定注视点** |
| **数据导出** | CSV 格式是否够用？还是 JSON 格式也需要？→ **默认 CSV** |
| **透明度** | 是否需要在按钮上标注字母/数字以便被试区分？→ **纯色块即可** |

> 💡 **建议**：先按照文档中的默认参数写一个最小可用版本，你拿一加 Ace 5 Pro 实际运行一次预实验，然后我们再根据实际情况调整参数。

---

## 10. 下一步

确认上述参数后，我会：

1. **Step 1**：编写完整代码（`index.html` + CSS + JS 文件）
2. **Step 2**：你部署到手机浏览器上运行测试
3. **Step 3**：根据预测试结果调整参数（按钮大小是否合适？试次数是否太多？等）
4. **Step 4**：生成正式版本并分发

---

## 附录：dp 与 CSS px 关系推导

### 错误推导（❌ 不要用）

```
Android dp 定义：1 dp = 1/160 inch
CSS 定义：      1 inch = 96 px (参考像素)

直观但错误的换算：
1 dp = 96/160 CSS px = 0.6 CSS px

问题：CSS 的 1 inch = 96 px 是"参考像素"定义，并非物理尺寸。
在手机浏览器上，CSS px 通过 DPR 映射到物理像素，而不是通过 96 PPI 换算。
```

### 正确推导（✅ 实际使用）

```
在 Android 手机上：

1. 系统将物理屏幕密度归入"密度桶"（density bucket）
   一加 Ace 5 Pro: 实际 450 PPI → 归入 xxhdpi (480) → DPR = 3.0

2. Android dp 的换算：
   1 dp = (densityDpi / 160) 个物理像素 = (480 / 160) = 3 个物理像素

3. 浏览器 CSS px 的换算：
   1 CSS px = DPR 个物理像素 = 3 个物理像素（因为 DPR 来自同一密度桶）

4. 结论：在同一个设备上：
   1 dp = 3 物理像素 = 1 CSS px ✓

5. 物理尺寸验证（一加 Ace 5 Pro，450 PPI，DPR = 3.0）：
   40 CSS px = 40 × 3 = 120 物理像素
   物理尺寸 = 120 / 450 inch = 0.267 inch = 6.76 mm
   标准 40 dp 预期 = 40 / 160 inch = 0.25 inch = 6.35 mm
   偏差 = (6.76 - 6.35) / 6.35 ≈ 6.5% ✓ （可接受）
```
